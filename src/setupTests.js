/**
 * Test environment setup entry point.
 *
 * Imports `@testing-library/jest-dom` to register custom Jest matchers for
 * asserting on DOM nodes. This enables expressive assertions such as:
 *
 *   expect(element).toHaveTextContent(/react/i);
 *   expect(element).toBeInTheDocument();
 *   expect(element).toBeVisible();
 *
 * @see {@link https://github.com/testing-library/jest-dom|jest-dom documentation}
 */

import '@testing-library/jest-dom';